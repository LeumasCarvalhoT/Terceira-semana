#include <cs50.h>
#include <stdio.h>

const int total = 3;

int main(void)
{
int notas[total];
 for (int i = 0; i < total; i++);
  {
    notas[i] = get_int ("Nota: ");
  }

  int x = notas[];

  printf ("Sua média é: %f\n", x / notas[total]);

}
